#include <gtest/gtest.h>

#include "../include/Cmem.h"

TEST(CMEM, init) {
    // we wanna test...that the parameters initialise correctly?
    // No, we want to test the public interface, that's all.
    // We wanna initialise and then test the GetSample() method I think. Jesus, got there eventually, didn't we?
    GTEST_ASSERT_EQ(2, 2); 
}